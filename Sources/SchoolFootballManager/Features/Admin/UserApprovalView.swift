import SwiftUI

struct UserApprovalView: View {
    var body: some View {
        NavigationView {
            Text("ユーザー承認画面")
                .navigationTitle("ユーザー管理")
        }
    }
}

#Preview {
    UserApprovalView()
}